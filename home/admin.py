from django.contrib import admin

from home.models import Child


# @admin.register(Child)
# class CustomerAdmin(admin.ModelAdmin):
#     list_display = ['name', 'date_of_birth', 'age', 'clothes_size']
